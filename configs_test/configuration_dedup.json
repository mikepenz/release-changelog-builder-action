{
    "sort": "ASC",
    "pr_template": "${{TITLE}}",
    "max_pull_requests": 1000,
    "max_back_track_time_days": 1000,
    "duplicate_filter": {
        "pattern": "([\\w-]+)",
        "on_property": "title",
        "method": "match"
    }
}